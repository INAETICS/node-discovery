{
    "package": {
        "name": "node-wiring-c", // Bintray package name
        "repo": "inaetics", // Bintray repository name
        "subject": "inaetics" // Bintray subject (user or organization)
    },

    "version": {
        "name": "latest"
    },

    "files":
        [
        {"includePattern": "deploy/(.*\.jar)", "uploadPattern": "$1", "matrixParams": { "override": 1 } }
        ],
    "publish": true
}
